package model;

public class Mobile implements IElement {
	private int posX;
	private int posY;
	
	public void Mobile(Position position, Map map) {
		this.posX = position.getX();
		this.posY = position.getY();
		while(map.get)
	}
}
